# Go HTTP Router

A lightweight, high-performance HTTP server template featuring native route handling in Go.

## Features

* **Zero Dependencies**: Uses only the Go standard library (`net/http`).
* **RESTful Routing**: Clean separation of API endpoints and HTTP methods.
